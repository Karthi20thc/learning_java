# ✅ **3. Class & Object Related Keywords**

These keywords are used for defining classes, inheritance, interfaces, and object creation.

---

## 🔹 **class**

Used to define a class.

```java
class Car {
    int speed;
}
```

---

## 🔹 **interface**

Used to define an interface (only abstract methods by default).

```java
interface Animal {
    void eat();
}
```

---

## 🔹 **enum**

Used to define a fixed set of constants.

```java
enum Direction { NORTH, SOUTH, EAST, WEST }
```

---

## 🔹 **extends**

Used for **inheritance** between classes.

```java
class Dog extends Animal { }
```

---

## 🔹 **implements**

Used when a class uses an **interface**.

```java
class Dog implements Animal { }
```

---

## 🔹 **new**

Used to **create objects**.

```java
Car myCar = new Car();
```

---

## 🔹 **this**

Refers to the **current object** (current class instance).

```java
this.speed = speed;
```

---

## 🔹 **super**

Refers to the **parent class**.

```java
super.run();
```

---

# 🟩 Summary Table: Class/Object Keywords

| Keyword    | Purpose                    |
| ---------- | -------------------------- |
| class      | Create a class             |
| interface  | Create an interface        |
| enum       | Create an enum             |
| extends    | Inherit from another class |
| implements | Use an interface           |
| new        | Create an object           |
| this       | Reference current object   |
| super      | Reference parent class     |

---

If you want, I can also:
✔ give real-world examples
✔ show a full program using all these keywords
✔ explain constructors, inheritance, polymorphism

Just tell me!